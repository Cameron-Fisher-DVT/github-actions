name: "Test Action"

on: push


jobs:
  echo-data:
    runs-on: ubuntu-latest
    steps:
      - name: Test Action
        run: |
          echo "Event name: ${{github.event_name}}"
          echo "Ref: ${{github.ref}}"
          echo "SHA: ${{github.sha}}"
          echo "Actor: ${{github.actor}}"
          